@model IEnumerable<GestionPedidos.Models.SalesChannel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SalesChannelName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SalesChannelName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalesChennelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalesChennelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalesChennelID })
        </td>
    </tr>
}

</table>
