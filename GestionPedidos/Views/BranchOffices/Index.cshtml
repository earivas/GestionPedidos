@model IEnumerable<GestionPedidos.Models.BranchOffice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BranchOfficeAddress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BranchOfficeAddress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BranchOfficeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BranchOfficeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BranchOfficeID })
        </td>
    </tr>
}

</table>
